#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		int putchar(n);
		int putchar(',');
		int putchar(' ');
		n++;
	}
	putchar('\n');
	return (0);
}
