#include "main.h"
#include <string.h>

/**
 * _strcmp - compares string and string 1
 * @s1: string 1
 * @s2: string 2
 *
 * Return: an int greater, equal to or less than 0
 */

int _strcmp(char *s1, char *s2)
{
	int val;
	val = strcmp(s1, s2);
	return (val);
}
