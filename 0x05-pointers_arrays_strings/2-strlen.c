#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @count - stores length of the string
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int count = strlen(*s);
	return (count);
}
