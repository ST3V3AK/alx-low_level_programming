#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: input pointer to string
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int count = strlen(s);

	return (count);
}
